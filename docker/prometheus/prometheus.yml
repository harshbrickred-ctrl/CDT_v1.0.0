# Prometheus scrape config for CDT API metrics (optional observability profile)
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: cdt-api
    static_configs:
      - targets: ['host.docker.internal:3000']
    metrics_path: /metrics
