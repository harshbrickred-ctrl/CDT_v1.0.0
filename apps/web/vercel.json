{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @cdt/shared-types build && pnpm --filter @cdt/shared-utils build && pnpm --filter @cdt/web build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
